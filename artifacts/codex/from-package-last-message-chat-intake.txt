Пакет был принят как единый source package, а primary-документ выбран по правилу manifest ranking: предложенный `PLAN_tech.md` оказался верхним кандидатом и единственным содержательным markdown-документом пакета. Это правило я зафиксировал в активном task note и дальше продолжил обычный execution-contract flow без повторного запуска governed launcher.

По реализации добавлен pilot anti-shortcut слой для двух critical contours: модель `target/staged/fallback`, config-driven contour detection, два новых fail-closed validator-а, встраивание в existing loop/pr flow, routing-сигналы для архитектурного и QA/acceptance review, а также два коротких acceptance passport-а. Важно, что critical mode теперь не срабатывает на docs-only сопровождение passport/checklist-файлов, а включается по реальным code/test surface пилотных контуров.

Проверка зелёная: прошли targeted tests для новых contour rules, `validate_task_request_contract`, `validate_session_handoff`, оба новых validator-а, `run_loop_gate.py --from-git --git-ref HEAD` и полный `run_pr_gate.py --from-git --git-ref HEAD`. Nightly gate я не гонял, чтобы не плодить лишний churn сверх уже обновлённых governance report artifacts.