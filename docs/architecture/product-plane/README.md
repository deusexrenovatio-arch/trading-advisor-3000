# Product Plane Architecture (Compatibility Pointer)

This directory is a temporary compatibility pointer introduced by
Phase 2 of the dual-surface rename migration.

Current canonical architecture docs still live under:

- `docs/architecture/app/`

Planned physical docs cutover:

- `docs/architecture/app/` -> `docs/architecture/product-plane/` (Phase 3)

Do not duplicate architecture content in both roots.
Use this pointer to guide contributors during dual-path compatibility mode.
