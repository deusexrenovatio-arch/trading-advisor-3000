# Archive Tasks

Archived notes are closed history, not active context.

Policy:
- Archived/completed notes are not inherited by default.
- Reuse is allowed only with explicit closeout evidence in the same diff:
  - `docs/session_handoff.md` update
  - matching archived note update

Index:
- `docs/tasks/archive/index.yaml` tracks completed lifecycle entries.
