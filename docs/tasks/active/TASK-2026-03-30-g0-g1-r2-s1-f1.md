# Task Note
Updated: 2026-03-30 10:08 UTC

## Goal
- Deliver: normalize the governed intake of the phase-acceptance and F1 full-closure package into a canonical F1 module contract and phase plan.

## Package Intake
- Package Type: single zip source package, not an already-clean specification.
- Manifest Suggested Primary: `01_phase_acceptance_verdict.md`
- Selected Primary: `03_f1_full_closure_TZ.md`
- Selection Rule: the manifest produced a top-score tie across multiple substantive markdown documents, so the primary was resolved explicitly by preferring the directive execution source that defines the F1 objective, ordered phases, hard rules, acceptance gates, disprovers, and release-decision semantics over verdict, findings, matrix, or evidence-manifest documents.
- Supporting Documents: `01_phase_acceptance_verdict.md`, `02_detailed_phase_findings.md`, `04_anti_self_deception_controls.md`, `05_source_evidence_manifest.md`, and `07_phase_status_matrix.json`.
- Mode Hint: `auto`, normalized to module-path continuation after contract creation.

## Task Request Contract
- Objective: convert the phase-driven F1 closure package into explicit governed contract and phase state without silently implementing multiple F1 closure phases in one intake patch.
- In Scope: `docs/codex/contracts/f1-full-closure.execution-contract.md`, `docs/codex/modules/f1-full-closure.parent.md`, the phase briefs under `docs/codex/modules/f1-full-closure.phase-*.md`, retirement of the old `stack-conformance-remediation` active pointer, and this active task note.
- Out of Scope: runtime implementation for `F1-A` through `F1-F`, truth-source or validator repair itself, Telegram or replaceable-stack closure, contracts-freeze delivery, sidecar proof hardening, broker connector work, and final release-decision regeneration.
- Constraints: do not call the governed launcher again; keep `docs/session_handoff.md` as a lightweight pointer shim; use canonical gate names only; treat the bundle as one package; keep one active module pointer after intake; preserve the package phase order; keep governance and future product patches separated; do not move trading logic into shell control-plane files.
- Done Evidence: the canonical execution contract and all declared F1 phase briefs exist under `docs/codex/`; the old `stack-conformance-remediation` parent no longer points to an active phase; `python scripts/validate_task_request_contract.py`; `python scripts/validate_session_handoff.py`; `python scripts/run_loop_gate.py --from-git --git-ref HEAD`.
- Priority Rule: quality and safety over speed when tradeoffs appear.

## Current Delta
- Read the required repo docs, the handoff pointer shim, the package manifest, the suggested primary document, and only the supporting package documents needed to resolve routing, blockers, and phase shape.
- Classified the package as phase-driven because the selected primary decomposes honest F1 closure into `F1-A` through `F1-F` with per-phase acceptance and disprover rules.
- Resolved the manifest tie explicitly instead of silently accepting the verdict document as execution primary.
- Chose a follow-on F1 module path and retired the earlier active pointer from `stack-conformance-remediation` so governed routing keeps one unambiguous current module.
- This run is limited to module-path normalization and phase planning, so no multi-phase F1 remediation implementation is folded into the intake patch.

## First-Time-Right Report
1. Confirmed coverage: the package clearly states the denied baseline, the F1 closure objective, the ordered `F1-A` through `F1-F` rollout, and the evidence hierarchy needed for phase planning.
2. Missing or risky scenarios: the manifest suggestion and the best execution source diverged because of a ranking tie, and creating a second active module without retiring the old pointer would make governed auto-routing ambiguous.
3. Resource/time risks and chosen controls: treat the bundle as one package, record the tie-break rule explicitly, create one execution contract plus atomic phase briefs, and stop before implementation work begins.
4. Highest-priority fixes or follow-ups: execute `F1-A` first to restore fail-closed truth-source and validator behavior before any downstream runtime closure phase starts.

## Repetition Control
- Max Same-Path Attempts: 2
- Stop Trigger: same failure repeats after two focused edits.
- Reset Action: pause edits, capture failing check, and reframe the approach.
- New Search Space: governed routing, module pointer integrity, validator contract coverage, and evidence alignment.
- Next Probe: run the smallest failing command before next patch.

## Task Outcome
- Outcome Status: in_progress
- Decision Quality: pending
- Final Contexts: pending
- Route Match: package intake matched and normalized into module path
- Primary Rework Cause: none
- Incident Signature: none
- Improvement Action: make the package's F1 closure rollout explicit before any new remediation implementation starts
- Improvement Artifact: docs/codex/contracts/f1-full-closure.execution-contract.md

## Blockers
- No blocker.

## Next Step
- Validate the normalized F1 contract state with the canonical checks, then hand the next governed unit of work to phase 01 (`F1-A` truth-source and validator repair).

## Validation
- `python scripts/validate_task_request_contract.py`
- `python scripts/validate_session_handoff.py`
- `python scripts/run_loop_gate.py --from-git --git-ref HEAD`
